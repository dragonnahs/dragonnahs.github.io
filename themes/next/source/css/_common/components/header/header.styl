.header { background: $head-bg; }

.header-inner { position: relative;
    //animation: left 1s ease;
 }
 @keyframes left{
     0%{
         left: -50%;
     }
     100%{
         left: 0%;
     }
 }


@import "headerband";
@import "site-meta";
@import "site-nav";
@import "menu";
